All we did was for classification, so should we change our research paper title to classification specific.

How do we change to py file instead of ipynb files.

adding masks as input  in embed() and predict() of BERT version caused error, worked for deberta version